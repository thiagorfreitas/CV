# CV

Site do currículo de Thiago Freitas — layout editorial (Instrument Serif + JetBrains Mono), com troca de idioma (PT/EN/ES), modo claro/escuro e download de PDF por idioma.

HTML/CSS/JS puro, sem build. Para rodar localmente:

```bash
python -m http.server 8000
```

e abrir `http://localhost:8000`.
